import type {FC} from 'react';
import {cardWrapper} from '../styles/common.ts';

export const AggregationTable: FC = () => {
  return (
    <div className="p-4">
      <div className={cardWrapper + ' h-full'}>234</div>
    </div>
  )
}
